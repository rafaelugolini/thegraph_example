{
  "name": "raf_playground",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ raf_playground",
    "create-local": "graph create --node http://localhost:8020/ raf_playground",
    "remove-local": "graph remove --node http://localhost:8020/ raf_playground",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 raf_playground"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
